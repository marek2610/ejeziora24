{% extends 'admin/admin_base.html.twig' %}

{% block title %}Województwo{% endblock %}

{% block page_title %}
    Województwo:
    <span class="text-primary">
        {{ region.wojewodztwo }}
    </span>
{% endblock %}

{% block body %}

<div class="table-responsive">
    <table class="table table-striped table-sm">
        <tbody>
            <tr>
                <th>Województwo</th>
                <td>{{ region.wojewodztwo }}</td>
            </tr>
        </tbody>
    </table>
</div>
    <a href="{{ path('admin.region.index') }}">powrót do listy województw</a>
    <br>
    {# <a href="{{ path('admin.region.edit', {'id': region.id}) }}">edytuj</a> #}

    {# {{ include('admin/users/_delete_form.html.twig') }} #}
{% endblock %}
