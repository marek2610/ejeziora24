{% extends 'admin/admin_base.html.twig' %}

{% block title %}Dane szczegółowe dla kategorii{% endblock %}

{% block page_title %}
    Dane szczegółowe dla kategorii  
    <span class="text-primary">
        {{ jeziora_kategorium.kategoria }}
    </span>

{% endblock %}

{% block body %}
<div class="table-responsive">
	<table class="table table-striped table-hover">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ jeziora_kategorium.id }}</td>
            </tr> #}
            <tr>
                <th>Kategoria</th>
                <td>{{ jeziora_kategorium.kategoria }}</td>
            </tr>
            <tr>
                <th>Opis</th>
                <td>{{ jeziora_kategorium.opis |striptags }}</td>
            </tr>
        </tbody>
    </table>
</div>

    <a href="{{ path('jeziora_kategoria_index') }}">Powrót</a>

    <a href="{{ path('jeziora_kategoria_edit', {'id': jeziora_kategorium.id}) }}">edit</a>

    {{ include('admin/jeziora_kategoria/_delete_form.html.twig') }}
{% endblock %}
