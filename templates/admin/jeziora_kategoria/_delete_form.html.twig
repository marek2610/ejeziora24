<form method="post" action="{{ path('jeziora_kategoria_delete', {'id': jeziora_kategorium.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jeziora_kategorium.id) }}">
    <button class="btn">Kasuj</button>
</form>
