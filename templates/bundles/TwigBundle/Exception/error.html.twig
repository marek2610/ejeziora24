{% extends "base.html.twig" %}

{% block title %}Error{% endblock %}

{% block body %}

<div class="px-3 py-3 pt-md-5 pb-md-4 mx-auto text-center">
	<h1 class="text-primary" style="font-size: 150px">{{ status_code }}</h1>
	<img src="{{ asset('uploads/logo/logo_head.png') }}" width="10%"/>
	<h2 class="text-primary">Strona nie istnieje</h2>
	{# <h5 class="text-primary">Błąd. Kod błędu {{ status_code }}</h5> #}
	<h5 class="text-primary">Komunikat błedu: {{ status_text }}</h5>

	{# <h5 class="text-primary">Wiadomość błedu: {{ exception.message }}</h5> #}
	{# trzeba dobrze skonfigurować komunikaty #}
	
	<p><a href="{{ path('jeziora_index') }}">Powrót do strony głównej</a></p>
</div>

{% endblock %}